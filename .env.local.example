NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
TWILIO_ACCOUNT_SID=
TWILIO_AUTH_TOKEN=
TWILIO_PHONE_NUMBER=
# Needed for: the local admin scripts in scripts/ (seed/cleanup/wipe test
# data) only. Never used by the Next.js app itself, never sent to the
# browser, never commit the real value.
SUPABASE_SERVICE_ROLE_KEY=
