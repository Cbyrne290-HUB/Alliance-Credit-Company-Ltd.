NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
TWILIO_ACCOUNT_SID=
TWILIO_AUTH_TOKEN=
TWILIO_PHONE_NUMBER=
# Needed for: the local admin scripts in scripts/ (seed/cleanup/wipe test
# data), and the login rate-limit check in src/lib/auth-actions.ts
# (signInAction — the only place in the app itself that uses this key,
# scoped to two narrow RPC calls via src/lib/supabase/rate-limit-client.ts).
# Never sent to the browser. Never commit the real value.
SUPABASE_SERVICE_ROLE_KEY=
